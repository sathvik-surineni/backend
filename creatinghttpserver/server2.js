// creating Rest Api using Express.
// Create "package.json"
// install "Express module"
// Create Express application
// Define routers to handle client request.
const exp=require("express")
const app=exp()
app.listen(3600,()=>console.log("Server Started from 3600..."))